import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // ← version complète avec toutes les routes

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
